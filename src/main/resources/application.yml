spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: lokeshdada47@gmail.com
    password: izdb lllw cbki aeha
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
      default-encoding: UTF-8

  profiles:
    active: dev

  application:
    name: socialMediainfluencers

  datasource:
    url: jdbc:postgresql://localhost:5432/influencer_db
    username: influencer_user
    password: 1234
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        jdbc:
          batch_size: 50
        order_inserts: true
        order_updates: true
        batch_versioned_data: true
logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE

Stripe:
  Secret:
    Key: your-stripe-secret-key-here